'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // Migration skipped as ownerId already exists
  },

  async down(queryInterface) {
    // Migration skipped as ownerId already exists
  },
};